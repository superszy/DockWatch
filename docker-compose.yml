services:
  dockwatch:
    build: .
    container_name: dockwatch
    restart: unless-stopped
    ports:
      - "3100:3000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - NODE_ENV=production
